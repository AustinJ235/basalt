use std::sync::Arc;

use vulkano::device::{Device, Queue};

use crate::window::BstWindowID;
use crate::Basalt;

pub fn run(basalt: Arc<Basalt>, window_id: BstWindowID, queue: Arc<Queue>) {
    todo!()
}
